string = ""

def function():
    userInput = input("Enter a sentance or word")
    newString = userInput + string
    arr = newString.split()
    for word in arr:
        letterCount = 0
        letters = enumerate(word)
        if letters == letters:
            letterCount == +1
        print(letterCount)


function()